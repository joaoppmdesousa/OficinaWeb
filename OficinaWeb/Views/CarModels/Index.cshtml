@model IEnumerable<OficinaWeb.Data.Entities.CarModel>

@{
    ViewData["Title"] = "Index";
}

<h2>@ViewBag.CarBrand Models</h2>

<p>
    <a asp-action="Create" asp-route-brandId="@ViewBag.CarBrandId" class="btn btn-success">Add Model</a>
    <a asp-controller="CarBrands" asp-action="Index"  class="btn btn-outline-primary">Back To Brands</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" asp-route-brandId="@ViewBag.CarBrandId" class="btn btn-outline-secondary">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" asp-route-brandId="@ViewBag.CarBrandId" class="btn btn-outline-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
