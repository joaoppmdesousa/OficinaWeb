@model IEnumerable<OficinaWeb.Data.Entities.CarBrand>

@{
    ViewData["Title"] = "Index";
}

<h1>Brands</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Add Brand</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-controller="CarModels" asp-action="Index" asp-route-brandId="@item.Id" class="btn btn-outline-primary">Models</a>
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-secondary">Edit</a> 
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
